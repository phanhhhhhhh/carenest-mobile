package com.carenest.backend.entity;

/**
 * A family member's self-set availability for daily notifications (UC A3).
 * Never consulted for SOS — SOS always pages everyone immediately.
 */
public enum AvailabilityStatus {
    FREE, BUSY
}
