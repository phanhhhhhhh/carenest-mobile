package com.carenest.backend.entity;

/** What kind of daily attention event started a Free Broadcast (UC A3). */
public enum BroadcastTriggerType {
    /** Elderly checked in feeling unwell (mood 3). */
    CHECK_IN_UNWELL
}
