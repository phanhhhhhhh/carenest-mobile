package com.carenest.backend.entity;

/** Kind of source row a Family Care Feed item (and its reactions) points at. */
public enum FeedItemType {
    CHECK_IN, MEDICATION_LOG, EMERGENCY, VISIT, CAMERA
}
